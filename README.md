# bash-genpass
A simple and lightweight password generator built using Bash scripting. This script uses openssl to generate secure passwords of a specified length.

# Features 🌟
-	Generates secure passwords using openssl.
-	Allows you to specify the length of the password.
- Easy to use and minimal dependencies.

# How It Works 🛠️
- The script prompts you to enter the desired password length.
- It generates two random passwords using openssl.
- The generated passwords are displayed on the screen.

# Usage

1. Clone the repository:
  ```bash
  git clone <repository-url>
  cd <repository-directory>
  ```

2. Make the script executable:
  ```bash
  chmod +x script.sh
  ```

3. Run the script:
  ```bash
  ./script.sh
  ```


# Why Use This? 🤔
- Quick and easy way to generate passwords.
- Utilizes OpenSSL for strong randomization.
- Fully customizable and lightweight.
